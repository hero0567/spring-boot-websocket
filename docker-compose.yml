version: '2'

services:
  websocket:
    image: levy/springbootwebsocket:v1.0.0
    restart: always
    container_name: websocket
    hostname: websocket
    ports:
      - "8086:8086"
    volumes:
      - /etc/timezone:/etc/timezone
      - /etc/localtime:/etc/localtime
